---
name: CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        shell: [bash]
    steps:
      - shell: ${{ matrix.shell }}
        run: |
          # shellcheck-finding
          echo $value
