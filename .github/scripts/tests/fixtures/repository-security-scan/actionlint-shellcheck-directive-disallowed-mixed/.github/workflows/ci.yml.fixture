---
name: CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: |
          # shellcheck shell=bash disable=SC2086
          echo $value
