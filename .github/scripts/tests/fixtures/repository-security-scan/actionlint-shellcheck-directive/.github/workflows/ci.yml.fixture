---
name: CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: |
          # shellcheck disable=all
          printf '%s\n' "$GITHUB_SHA"
