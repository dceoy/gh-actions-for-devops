---
name: CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-latest
    env:
      TOOL_VERSIONS: v1
    steps:
      - run: |
          # shellcheck disable=SC2153
          [[ "${TOOL_VERSION}" =~ ^v[0-9]+$ ]] || exit 1
